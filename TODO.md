# TODO

- [x] Update root `package.json` to add scripts to run frontend + backend together (via `npm run dev`)
- [x] Add `concurrently` as a root devDependency
- [x] Update root `README.md` with the new combined run command
- [x] Install dependencies and verify `npm run dev` starts both services



