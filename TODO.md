# Task: Remove hardcoded/placeholder fallbacks from chatbot and AI coach

## Steps
- [x] 1. Read and understand all relevant files
- [x] 2. Create plan and get user approval
- [x] 3. **ChatbotModule `handleSendWithText`**: Remove hardcoded keyword-based fallback, show proper error message
- [x] 4. **WellnessCoachDashboard insights `useEffect`**: Remove hardcoded fallback insights object, set null/error state (already done in a prior session)
- [x] 5. **WellnessCoachDashboard `handleGenerateRoutine`**: Remove hardcoded fallback routine, setRoutine(null) on error
- [x] 6. **WellnessCoachDashboard `handleAskCoach`**: Remove hardcoded fallback coach response, show error message
- [x] 7. ✅ All changes applied successfully
